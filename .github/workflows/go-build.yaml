name: Golang build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: run
      uses: cedrickring/golang-action@1.6.0
      env:
        PROJECT_PATH: "./cmd"